# Feature implementation status — 2026-09-22

**Integration `develop`:** product source still at original implementation; all new code is isolated on `feature/window-management`. **Draft PR:** https://github.com/hamthet/WinSidebar/pull/1 (head commit `c40b62f84a15b8378189e2fb5c590ed314baa023`). Do not infer that source changes already exist in `develop` or `main`, and do not merge to the public release.

**Implemented on feature branch (code candidate):** remove unconditional Calculator/Settings title/process filters; per-live-window displayed aliases with right-click Rename/Reset; persisted per-application ignore rules and reversible tray manager; `ATALHOS` heading with Save preferences and Restore defaults buttons. New helper `src/WindowManagement.cs` and updated `src/WinSidebar.cs`/`WinSidebar.csproj` are in commit above. A one-shot anchored patch script and feature-only workflow exist strictly as development tooling.

**Executed check:** GitHub Actions run https://github.com/hamthet/WinSidebar/actions/runs/35756515790 completed `success`: anchored source patch and `dotnet publish WinSidebar.csproj -c Release -r win-x64 --self-contained true -o publish` on Windows, then committed the resulting source. This proves a build, **not** Alt+Tab fidelity, no-phantom-window behavior, alias correctness under HWND reuse, persistence recovery, accessibility, layout or runtime localization. No manual Windows UI or five-language testing performed by the assistant.

**Open blockers before acceptance/integration:** WM-01..06 and SP-01..03 acceptance scenarios, especially true versus phantom Calculator/Settings, independently named five Calculators, UWP host/process identity, saving/restoring on denied writes and 211px/100–200% DPI. Five-language runtime catalogs are not yet implemented; current new strings are Portuguese. Alias lifetime is session/live-window only. Review any remaining mismatch against `CHANGE-REQUEST-2026-09-22.md` and fix on feature branch before marking PR ready.

**Owner constraints:** established preference behavior is OWNER-APPROVED (new controls not thereby approved); version migration testing OUT OF SCOPE; owner will test final five-language UI. The docs under `development/**` stay only on `develop` or feature branches, not final `main` or end-user ZIP. Existing v1.0.0 release remains Portuguese.