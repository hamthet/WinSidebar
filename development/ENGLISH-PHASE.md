# English product localization checkpoint (2026-09-22)

Status: NOT IMPLEMENTED; this document defines the next controlled phase. Source baseline: `feature/window-management` commit `c40b62f84a15b8378189e2fb5c590ed314baa023`. Target branch: `feature/runtime-i18n-en`. No release or documentation-first change.

Objective: implement reusable runtime locale selection and full English/Portuguese coverage for the existing product **including** the new window-management and shortcut preference controls. The Portuguese UI is the compatibility baseline; English is a complete new runtime catalog. Subsequent language stages add Spanish, Russian and Simplified Chinese using the same API without forking code. Never claim five-language support or ship a new release before all stages and owner UI testing pass.

Implementation acceptance: centralized stable text keys, resource catalogs embedded in one EXE; no translation of persistent IDs/process identities/user names/system window titles; menu choice available via tray and sidebar; manual locale saved without overwriting other preferences, Windows language auto-select on new installation, Portuguese choice for legacy settings without `language=`; all app-owned labels/messages/tooltips/accessibility strings in English (including WindowManagement.cs), refresh on switching language, translated default names only where non-destructive. Test C# build on Windows, locale key parity/placeholder compatibility, first run/restart/selection and functional regression. Catalog validation must fail on missing keys and avoid a misleading compile-only green light. Owner manually verifies runtime before acceptance.

Read `development/CHANGE-REQUEST-2026-09-22.md`, `development/PLAN.md` and `development/VERIFICATION.md` on `develop` for the authoritative product requirements. Update `development/LOG.md` on integration with observed evidence, distinguishing `PASS`, `NOT RUN` and `OUT OF SCOPE` migration tests. This checkpoint stays development-only and must not reach final main or the ZIP.
