# Russian runtime stage — owner feedback and remaining gates

2026-09-22. This document concerns the Russian locale. The subsequent Simplified Chinese work is tracked separately in [`RUNTIME-I18N-ZH-2026-09-22.md`](RUNTIME-I18N-ZH-2026-09-22.md). All project materials belong to `hamthet/WinSidebar`; private FILEBRIDGE is only temporary transfer for a specific owner-requested download. The owner confirmed GitHub Actions credits are unavailable. Work locally with Windows PowerShell, portable .NET and the standalone `C:\git\WinSidebar` clone; no admin or CMD is required.

## Implemented source and actual observations

- The Russian feature branch stacks on draft Spanish PR #3, which stacks on English/Portuguese PR #2 and window-management PR #1. None is merged into `develop` or `main`. The confirmed Russian head is [`67fe834`](https://github.com/hamthet/WinSidebar/commit/67fe83493bbe07861acfbfa55d076f922bff9280).
- [`i18n/ru-RU.json`](https://github.com/hamthet/WinSidebar/blob/feature/runtime-i18n-ru/i18n/ru-RU.json) contains translations for the 115 established app-owned keys. `src/Localization.cs` embeds and validates the four locales, detects Russian Windows UI and persists `language=ru-RU`. External titles, user names, paths and serialized identifiers remain unlocalized.
- The owner applied and pushed the five Russian selector changes in [`ed98ddd`](https://github.com/hamthet/WinSidebar/commit/ed98ddd). The later local UI patch was pushed in [`67fe834`](https://github.com/hamthet/WinSidebar/commit/67fe83493bbe07861acfbfa55d076f922bff9280): visible language control replaces redundant sidebar Save, while the editor's own Save/Cancel remains; the editor is positioned inside the monitor work area and marked TopMost.
- The owner reported **positive local testing of the four languages**. This is owner smoke feedback, not a complete per-locale test log. A previous screenshot showed the editor partly behind the sidebar and no visible language button; the later source patch addresses those two issues, but a separate explicit editor-foreground retest and language persistence result have not been supplied. The SDK installation log confirmed portable .NET 8.0.425; the Russian selector push was shown, but fresh test output/counts were not supplied in this conversation.
- The Russian branch's `build.yml` and `localization-tests.yml` have only manual `workflow_dispatch` triggers. The Spanish base branch retains a `pull_request` trigger; therefore **a Russian PR targeting Spanish has not been opened** while Actions are unavailable. Do not infer that Actions is disabled account-wide or repository-wide.

## Subsequent work and remaining checks

- [Chinese draft PR #4](https://github.com/hamthet/WinSidebar/pull/4) now targets the Russian branch, whose relevant workflows are manual-only. Its source resource/loader/tests are committed; its Chinese selector is awaiting owner-side local application/test. This draft is **not the Russian PR**. See the Chinese dossier for exact status.
- Russian and Chinese UI still require layout/font review at 211/260/324 px and high DPI. Functional acceptance separately requires Alt+Tab parity, real Calculator/Settings without ghost entries, individual aliases, ignored-app undo, auto-save and Restore including failures, monitor and hotkey behavior. Cross-version preference migration tests are OUT OF SCOPE by owner instruction.
- Before eventual shipping: safely address inherited workflow triggers, review dependent PR diffs, integrate only approved shipping source/resources/production tests, and exclude `development/**` and one-shot scripts from the clean release branch. Complete public five-language READMEs/tutorials/artwork after product validation; build/checksum locally and release a new version in WinSidebar. Historical `main`/v1.0.0 remain unchanged.
