# Russian runtime stage — source, owner testing and remaining gates

Date: 2026-09-22. All project source, resources, tests and documentation belong in `hamthet/WinSidebar`; private FILEBRIDGE is a temporary transfer only if the owner specifically needs an artifact to download and test. No new GitHub Actions runs: owner confirmed billing credits unavailable. Owner works in Windows PowerShell without administrator privileges, with the portable .NET 8 SDK in their user-controlled directory. The standalone WinSidebar clone is `C:\git\WinSidebar`, no longer nested inside ChatGPT Collector. Never put a personal local filesystem path into shipping source, public build defaults or release artifacts.

## Implemented and confirmed in GitHub

- `feature/runtime-i18n-ru` is stacked on the Spanish branch (draft PR #3), which is stacked on English/Portuguese (draft PR #2) and window management (draft PR #1). None is merged into `develop` or `main`. At the latest check the Russian branch head was [`67fe834`](https://github.com/hamthet/WinSidebar/commit/67fe83493bbe07861acfbfa55d076f922bff9280).
- [`i18n/ru-RU.json`](https://github.com/hamthet/WinSidebar/blob/feature/runtime-i18n-ru/i18n/ru-RU.json) includes translations for the 115 established application-owned catalog keys; `src/Localization.cs` loads and validates Russian alongside English/Portuguese/Spanish, detects Russian Windows UI language and persists `language=ru-RU`. Production/test project resources and four-language localization smoke source were updated. External window titles, personal data, paths, saved IDs and user-entered shortcut names must remain unchanged.
- Owner locally applied and pushed the five Russian-selector additions in [`ed98ddd`](https://github.com/hamthet/WinSidebar/commit/ed98ddd) (`src/WinSidebar.cs`). Later, in [`67fe834`](https://github.com/hamthet/WinSidebar/commit/67fe83493bbe07861acfbfa55d076f922bff9280), owner pushed the UI-feedback patch: visible globe-language button in place of redundant whole-form Save and an editor positioned within the monitor work area above the always-on-top sidebar. Shortcut editor's own Save/Cancel remains. This supersedes the earlier section stating that the Russian selector existed only as an unapplied script.
- Automatic `push`/`pull_request` triggers were removed from `build.yml` and `localization-tests.yml` on the **Russian branch only**. Do not infer Actions is disabled repository-wide: the Spanish base branch still has `pull_request` triggers, so opening a Russian PR could launch Actions. Check/disable relevant workflows before creating PR #4; do not spend unavailable credits.

## Actual owner feedback and verification limits

- Owner confirmed running a local preview and testing the languages, with positive feedback. Previously supplied screenshot showed Portuguese UI, an editor obscured by the sidebar and no visible language button; the subsequent source patch addresses those two issues. The precise locale-by-locale test matrix, console output, tests of the patched editor layout, language persistence after reboot and DPI/accessibility coverage have **not** been supplied. Record this as `OWNER SMOKE FEEDBACK: POSITIVE`, not as exhaustive five-language approval or a reproduced automated PASS.
- The earlier installer failure was due to missing `dotnet` in PATH; owner supplied a PowerShell log showing portable SDK 8.0.425 installed. Later user-provided `git push` output confirmed Russian selector commit `ed98ddd`; GitHub confirms `67fe834` on its remote branch. No Windows build logs or smoke check counts from the latest commit were provided in this conversation. Avoid asserting fresh PASS counts.
- Russian stage still lacks a draft PR. Simplified Chinese is not implemented. Functional acceptance is independent: genuine Alt+Tab windows without Calculator/Settings ghosts, five independent window aliases, reversible ignored apps, automatic preferences and Restore, multi-monitor/shortcuts, Windows 10/11, layout at narrow width/high DPI and keyboard accessibility. Cross-version migration tests are explicitly OUT OF SCOPE.

## Next actions

1. Implement Simplified Chinese runtime as a separate branch based on the current verified Russian branch. Preserve resource key parity and native language selector; run smoke/build locally. Avoid GitHub Actions and unnecessary FILEBRIDGE transfers.
2. Reconcile actual UI test results and residual layout/auto-save issues with owner; audit the per-locale strings for clipping and script font coverage.
3. Before opening the Russian/Chinese PRs, prevent inherited `pull_request` workflows on their base branches from launching. Review PR #1–#3, update summaries and prepare clean integration that excludes `development/**` and one-shot patch scripts from shipping `main`/ZIP.
4. Finish customer-facing five-language README/tutorial/artwork and versioned checksummed release only after functional product gates. Existing `main`/v1.0.0 remain untouched.
