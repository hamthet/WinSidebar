# Russian runtime stage — source, local validation and explicit blockers

Date: 2026-09-22. Owner directions: product first, one language per stage; no new GitHub Actions; `hamthet/WinSidebar` holds all project work, and private FILEBRIDGE is only an ephemeral `%TEMP%`-style transfer **if the owner actually needs an executable to test**. Do not copy plans, source or ongoing builds to FILEBRIDGE.

## Actual source work

- Created [`feature/runtime-i18n-ru`](https://github.com/hamthet/WinSidebar/tree/feature/runtime-i18n-ru) from Spanish branch commit [`2684d58`](https://github.com/hamthet/WinSidebar/commit/2684d586a050440c3ab3e6724ae0f2564c27af77); previous window-management and English/Portuguese/Spanish PRs #1–#3 are still drafts, not released.
- Removed `push` and `pull_request` triggers from the Russian feature branch's `build.yml` and `localization-tests.yml`. The inherited `feature-window-management.yml` only responds to its own feature branch, and `marketing-assets.yml` is limited to `main`. No GitHub Actions job was requested. GitHub's [Russian-branch run listing](https://github.com/hamthet/WinSidebar/actions?query=branch%3Afeature%2Fruntime-i18n-ru) returned zero runs at the last check; this is **not** an account billing balance.
- Added [`i18n/ru-RU.json`](https://github.com/hamthet/WinSidebar/blob/feature/runtime-i18n-ru/i18n/ru-RU.json), intended to cover all 115 established application-owned keys: UI, shortcut editor, validation/error messages, window renaming, reversible ignore rules, save/reset and accessibility text. Internal serialization, external window titles, user-entered names and paths remain unlocalized.
- Updated [`src/Localization.cs`](https://github.com/hamthet/WinSidebar/blob/feature/runtime-i18n-ru/src/Localization.cs) to load embedded Spanish/Russian catalogs with count/key checks and recognize Russian Windows UI languages, saved `language=ru-RU`, existing-profile Portuguese default and unsupported-locale English fallback. Updated both `.csproj` files to embed Russian inside the normal EXE and test EXE; updated `tests/LocalizationSmoke.cs` to cover four locales and reject not-yet-implemented Chinese.
- **Window-selector UI is not yet applied** to `src/WinSidebar.cs`: the development-only [`apply-runtime-i18n-ru.ps1`](https://github.com/hamthet/WinSidebar/blob/feature/runtime-i18n-ru/development/apply-runtime-i18n-ru.ps1) contains five fail-closed anchored replacements (field, native Русский menu label, click callback, menu insertion, checked state). It checks the exact existing source blob, requires a clean matching feature branch, then runs `dotnet` smoke tests and Windows build locally. `-Push` optionally commits/pushes **only after** successful local checks. Do not confuse the existence of this script with integrated or tested UI code. Remove the one-shot script from the final shipping branch, retaining this historical record in `develop`.

## Next local action and acceptance

On the owner's Windows PowerShell after reviewing the script: `gh repo clone hamthet/WinSidebar` (if not already cloned), `git fetch origin --prune`, `git switch --track origin/feature/runtime-i18n-ru` (or `git switch feature/runtime-i18n-ru` if already tracking), then `powershell -NoProfile -File .\development\apply-runtime-i18n-ru.ps1 -Push` **from the WinSidebar repository root**. Check displayed test/build output and pushed commit SHA before calling the Russian runtime integrated. The script performs no FILEBRIDGE upload. If the script fails, record the failure and do not mark tests passed or push a broken selector.

**Test status at dossier creation:** remote file commits were confirmed, but catalog JSON parity, PowerShell script execution, .NET smoke suite, Windows compile, Russian dropdown and interactive UI **NOT RUN/NOT VERIFIED in this stage**. No `gh` command ran on the owner's machine here. A manual Windows review must still assess Cyrillic clipping, DPI, screen readers, shortcut editor, genuine Alt+Tab windows, five Calculator aliases, ignored-app reversal and preference behavior. Cross-version migration tests remain OUT OF SCOPE. No customer release, `main` update or FILEBRIDGE file was created.
