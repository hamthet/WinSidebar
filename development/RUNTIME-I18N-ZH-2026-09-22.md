# Simplified Chinese runtime stage — implementation record, 2026-09-22

**Owner scope:** one locale per response; fifth locale `zh-CN` after English, Brazilian Portuguese, Spanish and Russian. WinSidebar repository owns ALL project code/docs/tests. Local checkout is `C:\git\WinSidebar`. No GitHub Actions credits; no Actions requested. FILEBRIDGE is a temporary transfer only if the owner specifically needs to download and test a file. No FILEBRIDGE transfer was used for this stage.

## Observed GitHub changes

- Created [`feature/runtime-i18n-zh`](https://github.com/hamthet/WinSidebar/tree/feature/runtime-i18n-zh) from Russian feature commit [`67fe834`](https://github.com/hamthet/WinSidebar/commit/67fe83493bbe07861acfbfa55d076f922bff9280), preserving the owner's tested four-language branch and later UI adjustment. [Draft PR #4](https://github.com/hamthet/WinSidebar/pull/4) targets **Russian feature branch** rather than `main` or `develop`; it is incomplete and must not be merged or released yet. A new PR to the Spanish base is NOT opened until its inherited `pull_request` Actions trigger is neutralized without consuming unavailable credits.
- Added [`i18n/zh-CN.json`](https://github.com/hamthet/WinSidebar/blob/feature/runtime-i18n-zh/i18n/zh-CN.json) with Chinese text for established application-owned keys; only Simplified Chinese is targeted. Source names/keys, external window titles, user names/paths, icon IDs and serialized identifiers remain unchanged.
- Updated [`src/Localization.cs`](https://github.com/hamthet/WinSidebar/blob/feature/runtime-i18n-zh/src/Localization.cs) to validate and merge a Chinese embedded resource alongside Spanish and Russian; `Select`/saved `settings.ini` accept `zh-CN`. A fresh Windows `zh-CN`, `zh-SG`, `zh-MY` or `zh-Hans*` UI selects Chinese; `zh-TW` / Traditional Chinese does not silently get Simplified. Existing settings with no `language` remain Portuguese.
- Updated `WinSidebar.csproj` and `tests/LocalizationSmoke.csproj` to embed the Chinese resource. Updated [`tests/LocalizationSmoke.cs`](https://github.com/hamthet/WinSidebar/blob/feature/runtime-i18n-zh/tests/LocalizationSmoke.cs) to exercise five locale lookups, matching catalog keys, first-run detection, saved selections and rejection of Traditional Chinese. The read-only localization workflow, which is **manual-dispatch-only**, has a five-language parity assertion for future use, but **was not run**.
- Added development-only [`apply-runtime-i18n-zh.ps1`](https://github.com/hamthet/WinSidebar/blob/feature/runtime-i18n-zh/development/apply-runtime-i18n-zh.ps1): a one-shot, fail-closed Windows PowerShell 5.1 script anchored to the Russian UI source blob. It checks current branch/clean checkout, static JSON key parity using native PowerShell (no Python), adds `简体中文` to the existing tray/sidebar/visible-globe language chooser using five source replacements, runs portable .NET smoke tests and full WinForms build, and optionally commits/pushes only after both pass. **The presence of this script does NOT mean `src/WinSidebar.cs` has the Chinese menu yet.**
- The [Chinese branch Actions listing](https://github.com/hamthet/WinSidebar/actions?query=branch%3Afeature%2Fruntime-i18n-zh) returned zero runs during this stage. The base branch's build and localization workflows have only manual `workflow_dispatch` triggers. This does not establish a billing balance or prevent unrelated workflows from being triggered elsewhere.

## Open test gates, not passed by a source commit

1. From a clean `C:\git\WinSidebar`, fetch and switch to the new Chinese feature branch, inspect script, run it **once** with `-Push` in Windows PowerShell. It locates portable SDK in the owner's user profile under `Desktop\HAMILTON_NAODELETAR\dotnet`. Record actual static parity output, number of executable smoke checks, Windows build status and new source commit. If the script stops after editing, do **not** rerun the patch; inspect local diff and resume only failed tests/push.
2. Close the running preview through its tray exit action, `dotnet publish` locally into an explicit temporary folder, run the exact produced executable, select visible globe → 简体中文, inspect side-panel/menu/editor/error strings, verify persistence after restart. Distinguish a user-approved workflow check from full acceptance: CJK font fallback, glyph rendering at 211/260/324 px and 100–200% DPI must be inspected in the real GUI.
3. Independently verify Alt+Tab parity, real Calculator/Settings windows without ghosts, five Calculator window aliases, ignored-app undo, defaults, monitors and hotkeys. Owner excluded cross-version migration tests (`OUT OF SCOPE`).
4. Only after product gates: reconcile internal docs; complete five-language PUBLIC READMEs, tutorials, artwork and in-package instructions; prepare clean release source excluding `development/**`, one-shot scripts and previews, build/checksum Windows deliverable locally and publish in `hamthet/WinSidebar`. Existing `main` / v1.0.0 is unchanged.

**Status:** source resources/loader/tests COMMITTED, Chinese UI menu awaiting local script, Chinese compile/smoke/GUI NOT RUN, PR #4 DRAFT. The owner's prior positive four-language UI feedback is recorded separately; it does not substitute for Chinese acceptance.
